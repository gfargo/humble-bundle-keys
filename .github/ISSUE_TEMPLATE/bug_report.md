---
name: Bug report
about: Report something that's broken (not a Humble redesign — see "Selector / DOM broken" for that)
title: '[bug] '
labels: bug
---

## What happened

<!-- One-sentence summary. -->

## Steps to reproduce

```bash
# The exact command(s) you ran:
humble-bundle-keys ...
```

## Expected behavior

<!-- What you thought would happen. -->

## Actual behavior

<!-- Logs, error messages, summary table contents. Paste from the run log if useful. -->

## Environment

- OS: <!-- macOS 14 / Ubuntu 22.04 / Windows 11 etc. -->
- Python: <!-- output of `python --version` -->
- humble-bundle-keys version: <!-- output of `humble-bundle-keys --version` -->
- Account type: <!-- Choice subscriber? Past-Monthly subscriber? Free user? -->

## Run log

<!-- The auto-generated run log lives at ~/.humble-bundle-keys/runs/run-<timestamp>.log
     Attach the relevant one (or paste excerpts).
     IMPORTANT: scan it before attaching — even though we redact known-sensitive
     headers in diagnose bundles, the standard run log captures real key values.
     If you paste keys, redact them first. -->
